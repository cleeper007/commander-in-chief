.DS_Store
Thumbs.db
*.log
node_modules/
__pycache__/
*.py[cod]

# Local beta-harness output. The private harness may write results.json relative
# to the CWD, so running it from the repository root can drop a multi-megabyte
# blob here. Public regression checks belong in tests/ and are intentionally
# tracked; agent/editor working state remains ignored below.
results.json
.vscode/
.idea/
.claude/

# Claude Code working file — local only, never published
CLAUDE.md
